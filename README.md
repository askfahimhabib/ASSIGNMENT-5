1️⃣ What is the difference between var, let, and const?
Ans: var was used before ES6 and it has hoisting issues and with it same variable can declare twice.
let and const both added in JS after ES6 update, in "Let" same variable won't declare twice but value can be reassign. can't be access from outside the block. one the other hand "const" can't be reassign, and its usage the most.

2️⃣ What is the spread operator (...)?
Ans: Using (...) we can expand, copy, or merge all elements of an array or object.

3️⃣ What is the difference between map(), filter(), and forEach()?
Ans: map() => makes new array from the existing array.
filter() => if any element passed the condition filter take them and make a new array with them.
forEach() => it's run loop and doesn't return anything.

4️⃣ What is an arrow function?
Ans: An arrow function is a short way to write a function in JS using _ => _ this. It's look like an
arrow and that's why it's called "Arrow Function".Arrow functions don't have their own 'this' binding.they inherit it from the parent scope.

5️⃣ What are template literals?
Ans: Template literals allow us to write multi-line strings easily and let us embed variables or expressions inside strings using ${}.
